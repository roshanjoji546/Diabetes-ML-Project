# Diabetes-ML-Project
Training ML techniques (KNN, Random Forest, Pruned Tree, Gradient Boosting) on a small diabetes dataset, then applying it to a test set to compare performance metrics (accuracy, error, F1 score, ROC-AUC etc...) in diabetes prediction.
